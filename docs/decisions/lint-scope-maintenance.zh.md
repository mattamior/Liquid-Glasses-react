# Lint 范围维护决策记录

状态：已采纳<br>
记录日期：2026-08-07

## 范围与决策

让仓库 lint 聚焦于受维护的应用源码。将生成的实验性 `output/**` 产物排除出 ESLint，
同时保留包括 V1 在内的全部应用路由处于 lint 范围内。V1 的单个
`react-hooks/set-state-in-effect` 结果只在源码行处豁免；它不会改变运行时行为，也
不是对整条规则的全局关闭。

## 已交付结果与改动区域

- [`eslint.config.mjs`](../../eslint.config.mjs) 全局忽略 `output/**`；该目录包含截图、
  打包资产和实验性 fixture，而不是可部署的应用源码。
- [`app/v1/page.tsx`](../../app/v1/page.tsx) 在延后的位移贴图初始化旁记录局部
  `react-hooks/set-state-in-effect` 豁免。V1 仍在仓库 lint 范围内。
- README 链接本记录，使 lint 约定及其例外可被发现。

## 验证证据

- 范围更新后，`npm run lint` 通过；没有扫描生成的 `output/**` 结果。
- `npm test` 通过：构建完成，SSR HTML 测试 5/5 通过。
- `git diff --check` 通过。

## 发布状态

这是本地 lint 策略和文档更新。本决策没有执行推送、部署、公开路由变更或生产配置修改。

## 已知限制与后续工作

- 下次修改 V1 源码时，应将基于 effect 的位移贴图初始化替换为渲染安全的初始化方案，
  并移除该行级豁免。
- 不要扩大 V1 豁免范围，也不要排除 `app/v1/**`；V1 的新 lint 结果必须保持可见，
  除非存在单独记录的决策。
- 生成产物被有意排除出 lint，但必须继续处于可部署应用源码之外，且不应作为生产代码
  提交。
